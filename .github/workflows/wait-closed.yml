name: Wait Workflow
on: pull_request

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Wait for 180 seconds
        run: |
          echo "Waiting for 70 seconds..."
          sleep 70
          echo "Wait completed!"
